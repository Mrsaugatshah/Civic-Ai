import test from "node:test";import assert from "node:assert/strict";import {analyzeWithProvider} from "./ai-service.js";
test("AI provider output is enum and range validated",async()=>{Object.assign(process.env,{AI_PROVIDER:"openai-compatible",AI_API_KEY:"test",AI_MODEL:"test",AI_BASE_URL:"https://provider.invalid/v1"});const original=globalThis.fetch;globalThis.fetch=async()=>({ok:true,json:async()=>({choices:[{message:{content:JSON.stringify({category:"pothole",priority:"high",department:"Roads & Infrastructure",confidence:.92,summary:"A dangerous pothole needs prompt inspection."})}}]})});try{const result=await analyzeWithProvider({title:"Pothole",description:"Deep road hole",category:"pothole",address:"Ward 11"});assert.equal(result.priorityScore,78);assert.equal(result.category,"pothole");}finally{globalThis.fetch=original;}});
test("invalid AI output is rejected",async()=>{Object.assign(process.env,{AI_PROVIDER:"openai-compatible",AI_API_KEY:"test",AI_MODEL:"test"});const original=globalThis.fetch;globalThis.fetch=async()=>({ok:true,json:async()=>({choices:[{message:{content:'{"category":"invented","priority":"extreme","confidence":99,"summary":"bad"}'}}]})});try{await assert.rejects(()=>analyzeWithProvider({}),/invalid category or priority/);}finally{globalThis.fetch=original;}});
